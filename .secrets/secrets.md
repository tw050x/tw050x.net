# Secrets

Below is a list of all required secrets within the platform:

- `encryption.cipher.secret-key`: Secret key used for encryption and decryption processes.
- `mongo.client.auth-password`: Password for MongoDB client authentication.
- `oauth2.google.client-secret`: Google OAuth2 Client Secret for authentication.
